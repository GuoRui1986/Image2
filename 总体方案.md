# 多米生图内部平台 · 总体方案（定稿 V5 · 接口规格已实测确认）

> 状态：**方案彻底封板**。功能层、部署层已定；多米 4 个接口的提交/查询/参数/返回结构已于 2026-07-25 用真实 API Key 全部实测跑通（nano 文生图+图生图、gpt 文生图+图生图均成功出图）。
> 本文件第 11 节即实施阶段可直接照抄的接口契约。
> 最后更新：2026-07-26（存储改为只存多米 URL；图生图参考图已定走 Supabase Storage 临时桶、生成后删除；前端写在一起 SPA 单页）
> 注：旧 Flask 草稿（app.py / static 等）已删除，按本方案 Node.js 实施。

## 1. 目标与范围
- 内部 **2-3 人（异地）** 使用，通过「多米」中转接口调用 **GPT Image 2 / Nano Banana** 生成商品图。
- 两个端：**管理端**（账号/积分/计费/记录/日志/统计） + **用户端**（登录后生图，含批量）。
- 不做：对外商业化、多租户、支付收银、短信验证（账户由管理员创建）。

## 2. 部署架构（✅ 已定）
- **后端**：**Node.js + Express**，部署到 **火山引擎 IGA Pages**（DCDN 全站加速，国内节点、公测免费）。
- **数据库**：**火山引擎 Supabase**（国内版，Postgres，数据不出境）。**图不落盘、不引 Storage**——仅持久化多米返回的图 URL（约 5 天过期，接受）。
- **前端**：原生 HTML/JS，**写在一起（单项目 SPA 单页 `index.html`，用户端 `/` 与管理端 `/admin` 视图切换）**，随 IGA Pages 部署。
- **鉴权**：自建 `users` 表 + bcrypt + JWT。
- **异步模式**：因边缘函数有执行超时，改为 **前端驱动轮询**。

```
┌─────────────┐      ┌──────────────────────────┐      ┌──────────────┐
│  管理端 Web  │      │  火山 IGA Pages (Node Fn)  │      │   多米 API    │
│  (/admin)   │─────▶│  · 鉴权(手机号+密码/JWT)   │─异步─▶│ (异步生图)    │
├─────────────┤      │  · 账户/积分/计费/记录/日志 │      └──────┬───────┘
│  用户端 Web  │─────▶│  · 生图代理(提交/查状态)   │             │ task_id
│  (/)        │      └───────────┬──────────────┘             ↓
└─────────────┘     前端定时轮询 │ 查状态→拿图URL      轮询拿图 URL
                           ┌──────┴───────┐          (仅存图URL,5天过期)
                           │ 火山 Supabase │◀──── 写记录(图URL)
                           │   Postgres    │ (国内,不出境)
                           └──────────────┘
```

## 3. 功能模块

### 3.1 管理端（/admin，admin 角色）
| 功能 | 说明 | 状态 |
|------|------|------|
| 账户管理 | 创建账户（手机号+密码）、列表、冻结/启用、删除、重置密码 | ✅ |
| 积分管理 | 给账户充值 / 扣减「可用积分」余额 | ✅ |
| 计费规则 | 设置每种生图方式扣多少积分（**4 种**，见 §4） | ✅ |
| 记录查看 | 所有用户生图记录：谁/方式/prompt/参考图/结果图/消耗/成败/时间，可按用户·时间·方式筛选 | ✅ |
| 操作日志 | 记录谁改了账户/积分/计费（admin_id·action·target·detail·时间） | ✅ |
| 使用统计 | 总消耗、各方式使用量、各用户消耗排名 | ✅ |

### 3.2 用户端（/ 登录后）
| 功能 | 说明 | 状态 |
|------|------|------|
| 登录/登出 | 手机号 + 密码 | ✅ |
| 引擎切换 | image2（GPT Image 2）/ nano banana 两个 Tab | ✅ |
| 文生图 | prompt(必填) · 模型 · **比例** · **分辨率/尺寸** · **数量(循环提交)** · 节点(国内/海外) | ✅ 参数已实测确认（§11） |
| 图生图 | **支持多张参考图上传**（image2 ≤多图 / nano ≤10 张） + prompt + 模型 + 比例 + 尺寸 + 节点 | ✅ 两种引擎均支持图生图（gpt 实测出图） |
| 批量生图 | 一次填多个 prompt，前端依次提交并轮询 | ✅ |
| 节点选择 | 文生图时可切「国内 / 海外」节点（oversea 参数，gpt 支持；nano 待核实，见 §12） | ✅ gpt 支持 |
| 出图 | 前端轮询到完成 → 预览 + 下载（直链多米 URL，不落存储） | ✅ |
| 个人中心 | 我的剩余积分、我的生图历史（缩略图+prompt+消耗） | ✅ |

**模型选项（model）**
- image2（gpt-image-2）：`gpt-image-2`（唯一）
- nano banana（4 版）：`gemini-2.5-flash-image`(老版/nano-banana,¥0.07) / `gemini-3.1-flash-image-preview`(nano-banana-2,¥0.07) / `gemini-3-pro-image-preview`(nano-banana-pro,¥0.14) / 别名 nano-banana·nano-banana-2·nano-banana-pro 也可用

## 4. 生图方式（计费维度，共 4 种）
| 标识 | 引擎 | 模式 | 多米接口 | 备注 |
|------|------|------|----------|------|
| image2_t2i | GPT Image 2 | 文生图 | `POST /v1/images/generations?async=true` | 参数见 §11.4 |
| image2_i2i | GPT Image 2 | 图生图 | 同上 + `image[]` 参数 | **实测可出图**，恢复此方式 |
| nano_t2i  | Nano Banana | 文生图 | `POST /api/gemini/nano-banana` | 参数见 §11.2 |
| nano_i2i  | Nano Banana | 图生图 | `POST /api/gemini/nano-banana-edit` | 参数见 §11.3，`image_urls[]`≤10 |

## 5. 数据模型（火山 Supabase Postgres，国内）
- **users**：`id` / `phone`(唯一) / `password_hash`(bcrypt) / `balance`(积分) / `role`(admin|user) / `status`(active|frozen) / `created_at`
- **pricing_rules**：`id` / `type`(image2_t2i|image2_i2i|nano_t2i|nano_i2i) / `cost`(每次扣积分)
- **generation_logs**：`id` / `task_id`(多米任务ID,唯一) / `user_id` / `method` / `prompt` / `ref_images`(URL数组,图生图可选) / `result_image`(URL,多米返回) / `cost` / `status`(success|fail) / `oversea`(bool) / `params`(JSON: 比例/分辨率/数量等请求参数留痕) / `created_at`
- **operation_logs**：`id` / `admin_id` / `action` / `target_type` / `target_id` / `detail` / `created_at`

> 使用统计由 `generation_logs` 聚合得出。`params` 记录每次生图的比例/尺寸/数量，便于管理端追溯。

## 6. 生图流程（前端驱动轮询，适配边缘函数超时）
1. 用户端提交（引擎+模式+prompt+比例+尺寸+数量，图生图附多张参考图，可批量；节点可选国内/海外）
2. 后端（Node Fn）校验：JWT 登录态 ✓ → 查该方式 `cost` → 余额足够？
3. 调用多米对应接口**提交**（携带 §11 确认的参数名与鉴权方式）→ 返回 `task_id`（gpt 称 `id`）
4. **前端每 3–5 秒** 调 `/api/status?task_id=xxx`，后端按引擎调用对应的查询接口（nano 用 `?id=&key=`、gpt 用 `/{id}`）返回 pending/running/done+图URL
5. 完成后：后端把多米返回的图 **URL 直接写入 `generation_logs`**（含 params 留痕）；积分已在提交时**预扣**，失败/部分失败时**返还**（URL 约 5 天过期，接受，不下载落盘）
6. 前端展示结果图 + 提供下载
7. **数量 > 1**：前端循环调用 /api/generate N 次（每次独立任务）（每次独立 task_id、独立预扣、独立记录），前端分别显示每个任务进度
8. 批量：多个 prompt 各自走 3–7

## 7. 技术栈（✅ 已定）
- 后端：Node.js + Express，部署火山 IGA Pages（边缘函数，前端驱动轮询）
- 数据库：火山引擎 Supabase Postgres（国内版）
- 图存储：仅参考图上传用 Supabase 临时桶（生成后删除），结果图只存多米返回 URL（约 5 天过期接受，不落盘，不引永久 Storage 成本）
- 前端：原生 HTML/JS，单项目写在一起，SPA 单页（用户端/管理端视图切换）
- 鉴权：自建 users 表 + bcrypt + JWT
- 积分：内部虚拟额度，管理员自由设每种方式扣分值
- 失败扣费：提交时预扣，失败/部分失败按张返还
- 管理员：单一，种子脚本/环境变量初始化
- 图像下载：不落盘，前端直链多米 URL 下载（规避存储成本）

## 8. 关键决策记录（全部已定）
| # | 决策点 | 结论 |
|---|--------|------|
| 1 | 积分机制 | 内部虚拟额度，每种方式独立设扣分 |
| 2 | 图存储 | 只存多米返回 URL（约 5 天过期，接受）；不引 Storage，省成本 |
| 3 | 管理员 | 单一，种子脚本创建 |
| 4 | 部署 | Node.js + 火山 IGA Pages + 火山 Supabase |
| 5 | 登录 | 自建手机号+密码，无短信验证 |
| 6 | 失败扣费 | 提交预扣，失败/部分失败返还 |
| 7 | 参考图出境 | OK（图生图参考图发往多米中转，属已接受范围） |
| 8 | 规模 | 2-3 人 |
| 9 | 用户端增强 | 批量生图 + 节点切换 |
| 10 | 管理端增强 | 操作日志 + 使用统计 |
| 11 | oversea | gpt 暴露给用户选（默认海外）；nano 待核实 |
| 12 | 后端语言 | Node.js/Express（IGA Pages 仅支持 Node 运行时） |
| 13 | 异步模式 | 前端驱动轮询（边缘函数超时约束） |
| 14 | 用户端控件 | 比例 / 尺寸 / 数量；数量用前端循环调用 /api/generate N 次（每次独立任务，接口无 n 参数） |
| 15 | nano model | 4 版（老版/2/pro），按价格区分 |
| 16 | **图生图多图** | image2(gpt image[]) / nano(image_urls[]≤10)，底层均支持多图融合 |
| 17 | **image2 图生图** | **恢复**（gpt-image-2 实测支持 `image` 参数图生图）→ 4 种生图方式 |
| 18 | **查询接口** | nano：`GET /api/gemini/nano-banana/result?id={}&key={}`；gpt：`GET /v1/tasks/{id}`，两套分别实现 |
| 19 | **图 URL 有效期** | 多米返回图 URL 带 token，约 5 天过期 → 接受过期，只存 URL 不落盘 |

## 9. 风险与合规
- **中转稳定性**：多米曾出现模型故障/全挂，停摆是真实风险。内部自用+普通图可接受，核心交付别全押一家（后续可加备用 provider）。
- **数据合规**：✅ 数据库与账户存火山国内 Supabase，不出境；生图结果仅存多米 URL（过期接受，不影响账户/记录合规）。
- **边缘函数超时**：生图轮询改前端驱动规避；后端不再下载存图，单次请求轻量。
- **公测免费风险**：IGA Pages 公测期全免，商用后计费待定，留意公告。
- **备案**：内部 2-3 人用 IGA Pages 测试域名即可，无需 ICP 备案；绑自定义域名需备案。
- **密码安全**：bcrypt 哈希，JWT 加密传输（HTTPS）。
- **图 URL 过期**：接受多米 URL 5 天过期，仅持久化 URL，前端直链下载，不落盘。

## 10. 实施步骤（建议）
1. 建火山 Supabase 项目（4 张表，无需 Storage bucket）+ IGA Pages 项目
2. 后端（Node.js/Express）：鉴权、admin 种子、账户/积分/计费/记录/日志 CRUD
3. **多米生图适配层（4 种方式，按 §11 实测规格实现）**：提交(两套鉴权) + 查询(两套接口) + 存 URL + 扣积分 + 数量循环提交
4. 前端：用户端（登录+文生图[含比例/尺寸/数量]+图生图[多图]+批量+节点切换+轮询出图+下载+个人中心）、管理端
5. 联调 + 用真实 Key 跑通第一张图（接口规格已验证，直接按 §11 实现即可）

## 11. 多米接口规格（实测确认，实施直接照抄）

### 11.0 通用注意
- **两套鉴权并存**：nano 系列「提交用 `Authorization` 头、查询用 `?key=` 参数」；gpt 系列「提交与查询都用 `Authorization` 头」。
- **查询参数名不同**：nano 查询用 `?id=`；gpt 查询用路径 `/{id}`。
- **返回结构不同**：nano 包裹 `code/msg/data`（图片在 `data.data.images[].url`）；gpt 顶层 `id/state/data`（图片在 `data.images[].url`）。
- 任务状态值：nano `data.state` ∈ {running, succeeded, ...}；gpt `state` ∈ {pending, running, succeeded, error}。
- 图片 URL 带 token，约 5 天过期，仅持久化 URL（过期接受），不下载落盘。

### 11.1 nano 文生图（nano_t2i）
- 提交：`POST https://duomiapi.com/api/gemini/nano-banana`
  - Header：`Authorization: <KEY>`
  - Body(JSON)：`{ "prompt":"...", "model":"gemini-3.1-flash-image-preview", "image_size":"1K", "aspect_ratio":"1:1" }`
  - 返回：`{ "code":200, "data":{ "task_id":"<uuid>" } }` → 取 `data.task_id`
- 查询：`GET https://duomiapi.com/api/gemini/nano-banana/result?id=<task_id>&key=<KEY>`
  - 轮询至 `data.state=="succeeded"`，图片取 `data.data.images[0].url`
- 参数枚举：
  - `model`：gemini-2.5-flash-image / gemini-3.1-flash-image-preview / gemini-3-pro-image-preview（或别名 nano-banana / nano-banana-2 / nano-banana-pro）
  - `image_size`：1K / 2K / 4K（K 大写）
  - `aspect_ratio`：auto / 1:1 / 2:3 / 3:2 / 3:4 / 4:3 / 4:5 / 5:4 / 9:16 / 16:9 / 21:9（默认 auto）
  - `n`（数量）：**接口不支持，固定 1 张** → 数量用循环提交

### 11.2 nano 图生图（nano_i2i）
- 提交：`POST https://duomiapi.com/api/gemini/nano-banana-edit`
  - Header：`Authorization: <KEY>`
  - Body(JSON)：`{ "prompt":"...", "model":"gemini-3.1-flash-image-preview", "image_urls":["https://.../a.png","https://.../b.png"], "aspect_ratio":"16:9", "image_size":"1K" }`
  - 返回：`{ "code":200, "data":{ "task_id":"<uuid>" } }`
- 查询：同 §11.1 的查询接口（`/api/gemini/nano-banana/result?id=<>&key=<>`）
- 参数：
  - `image_urls`：**array[string]，多张参考图，最多 10 张，仅支持链接形式（不支持 base64）**
  - `aspect_ratio`：单参考图建议不传（自适应）；多图可传
  - `image_size`：1K/2K/4K（nano pro 和 nano 2 支持）
  - 其他同 §11.1

### 11.3 gpt 文生图（image2_t2i）
- 提交：`POST https://duomiapi.com/v1/images/generations?async=true`
  - Header：`Authorization: <KEY>`
  - Body(JSON)：`{ "model":"gpt-image-2", "prompt":"...", "size":"1024x1024", "quality":"medium", "oversea":false }`
  - 返回：`{ "id":"<uuid>" }` → 取顶层 `id`
- 查询：`GET https://duomiapi.com/v1/tasks/<id>`
  - Header：`Authorization: <KEY>`
  - 轮询至 `state=="succeeded"`，图片取 `data.images[0].url`
- 参数枚举：
  - `model`：gpt-image-2（唯一）
  - `size`：auto / 1024x1024 / 1792x1024 / 1024x1792 / 1:1 / 3:2 / 2:3 / 16:9 / 9:16 / 2:1 / 1:2 / 4:3 / 3:4 / 5:4 / 4:5，亦支持自定义 `宽x高`（宽高均能被 16 整除，单边 16–3840）
  - `quality`：low / medium / high（思考深度，可选）
  - `oversea`：bool（可选，节点选择；默认 false=国内）
  - `n`（数量）：**接口不支持，固定 1 张** → 数量用循环提交

### 11.4 gpt 图生图（image2_i2i）
- 提交：同 §11.3，Body 增加 `image` 参数：
  - `{ "model":"gpt-image-2", "prompt":"...", "size":"1024x1024", "image":["https://.../a.png","https://.../b.png"], "oversea":false }`
  - `image`：**string 或 array[string]，多张参考图用数组**
- 查询：同 §11.3（`GET /v1/tasks/<id>`）
- **已实测可出图**（2026-07-25）

### 11.5 数量（n）处理
- 多米两个模型接口均无 `n` 参数，每次固定返回 1 张。
- 用户端「数量」控件语义 = **前端循环调用 /api/generate N 次（每次独立任务）**：每次独立 task_id、独立查询、独立存 URL（不落盘）、独立预扣、独立写记录。前端分别展示每个子任务进度。

### 11.6 适配层实现要点（Node.js）
- 抽象 `submit(task)` / `query(taskId)` 两个方法，按 `method` 分流到 nano / gpt 两套实现。
- nano 提交用 `Authorization` 头；nano 查询用 `?key=` 参数 + `?id=`。
- gpt 提交/查询均用 `Authorization` 头；查询路径 `/v1/tasks/{id}`。
- 轮询：前端每 3–5 秒查一次；后端设最大等待（如 180 秒）防卡死。
- 存 URL：拿到图片 URL 后直接持久化到 `generation_logs`，前端直链下载，不在我方落盘。

## 12. 待确认（仅剩 minor）
- **nano 的 `oversea` 参数**：gpt 明确支持 `oversea`；nano edit 文档未列 `oversea`，nano 文生图是否支持待核实（若不支持，nano 固定走国内节点，节点切换仅对 gpt 生效）。不影响 4 种方式核心实现。
- 火山 Supabase 实际产品开通入口/名称，实施时按控制台实际界面操作。
- IGA Pages 公测转商用后的计费，留意官方公告。
- **图生图参考图托管（已定）**：多米 nano/gpt 图生图接口只接受图片 **URL**（不支持 base64）。已定：用户上传参考图 → 前端传 **Supabase Storage 临时桶** → 取公开 URL 传给多米 → 生成成功后删除该临时图（几 MB 几乎零成本，与「结果图不存」不冲突）。
