# 两端功能与流程（理清版）

> 基于《总体方案 V5》整理，部署=火山 IGA Pages + 火山 Supabase(仅 Postgres)，生图走多米中转。
> 存储原则：**只存多米返回的图 URL（约 5 天过期，接受过期），不下载、不引 Storage**。

---

## 一、用户端（`/`，手机号+密码登录后）

### 1. 登录 / 登出
| 元素 | 说明 |
|------|------|
| 手机号、密码输入框 | 必填；账户由管理端创建，无注册 |
| 登录按钮 | bcrypt 校验，失败提示"手机号或密码错误" |
| 登出 | 顶栏，清除本地 JWT |

### 2. 主工作台
| 区域 | 元素 | 说明 |
|------|------|------|
| 顶栏 | 手机号 + 剩余积分 + 登出 | 实时显示余额 |
| 引擎 Tab | `image2`(GPT Image 2) / `nano banana` | 切换 |
| 模式 | `文生图` / `图生图` | 两个引擎都支持两种模式 |
| 文生图 | prompt(必填) · 模型 · 比例 · 分辨率/尺寸 · 数量(1-10) · 节点(国内/海外,默认海外) | 见下方「参数对照」 |
| 图生图 | 参考图上传(多张) + prompt + 模型 + 比例 + 尺寸 + 节点 | 参考图托管方式待定（见 §四） |
| 批量 | "加一条 prompt" | 可叠多个 prompt，逐个提交 |

**参数对照（实测确认）**
| 参数 | image2 (gpt-image-2) | nano banana |
|------|----------------------|-------------|
| 模型 | `gpt-image-2`（唯一） | 4 版：`gemini-2.5-flash-image`(老版,¥0.07) / `gemini-3.1-flash-image-preview`(2,¥0.07) / `gemini-3-pro-image-preview`(pro,¥0.14) / 别名 nano-banana·nano-banana-2·nano-banana-pro |
| 比例/尺寸 | `size`：1:1 / 16:9 / 1024x1024 / 1792x1024 等（含自定义 宽x高） | `aspect_ratio`：1:1/16:9/3:4…（默认 auto）；`image_size`：1K/2K/4K |
| 数量 | 接口无 `n`，固定 1 张 → 前端**循环调用 `/api/generate` N 次（N≤10）** | 同左 |
| 节点 | `oversea`(bool，默认 false=国内)，**暴露给用户选** | 文档未列 oversea（待核实，不支持则固定国内） |

### 3. 生成区
| 元素 | 说明 |
|------|------|
| 任务卡片列表 | 每个 prompt / 每次循环 = 一张卡：状态(生成中→成功/失败)、预览图、下载按钮 |
| 前端轮询 | 每 3–5 秒查 `/api/status?task_id=`（边缘函数超时，故前端驱动） |
| 失败 | 显示原因，**不扣积分** |
| 下载 | 直链多米 URL（前端 `<a download>` 或新窗口打开） |

### 4. 个人中心
| 元素 | 说明 |
|------|------|
| 剩余积分 | 实时 |
| 我的历史 | 缩略图 + prompt + 方式 + 消耗 + 时间，可下载（直链） |

---

## 二、管理端（`/admin`，admin 角色登录后）

| 模块 | 页面元素 | 操作 |
|------|---------|------|
| 仪表盘 | 总消耗、各方式使用量、用户数 | 只读 |
| 账户管理 | 创建：手机号+密码+确认；列表：手机号/角色/状态/积分/创建时间 | 创建、冻结/启用、重置密码、删除 |
| 积分管理 | 选账户 → 充值 / 扣减 → 填数额 | 调整余额（写操作日志） |
| 计费规则 | 4 个输入框：image2文生图 / image2图生图 / nano文生图 / nano图生图 各自扣 X 分 | 保存（写操作日志） |
| 生图记录 | 表格：用户/方式/prompt/参考图/结果图/消耗/状态/时间 | 筛选：按用户·时间·方式 |
| 操作日志 | 表格：时间/管理员/动作/对象/详情 | 只读 |
| 使用统计 | 总消耗、各方式次数与消耗、各用户消耗排名 | 只读 |

> 4 种计费维度：`image2_t2i` / `image2_i2i` / `nano_t2i` / `nano_i2i`（gpt 图生图已实测恢复）。

---

## 三、主流程

### 1. 登录流程
手机号 + 密码 → bcrypt 校验 → 签发 JWT → 前端存 token → 按 `role` 进对应端。

### 2. 生图流程（用户端）
1. 选引擎/模式 + 填 prompt/参数 + 图生图附多张参考图（可批量、可设数量）
2. 后端校验：JWT 登录态 ✓ → 查该方式 `cost` → 余额足够？
3. 调多米对应接口**提交** → 返回 `task_id`（gpt 称 `id`）
4. **前端每 3–5 秒** 查 `/api/status` → 后端按引擎调对应查询接口（nano:`?id=&key=` / gpt:`/{id}`）→ 返回 pending/running/done+图URL
5. 完成：把多米图 **URL 写入 `generation_logs`**（含 params 留痕）→ **仅成功才扣积分**
6. 前端显示结果图 + 提供下载（直链 URL）
7. **数量 > 1**：前端循环调用 /api/generate N 次（每次独立任务），每次独立 task_id / 查询 / 存 URL / 扣费 / 记录，前端分别显示进度
8. 批量：多个 prompt 各自走 1–7

### 3. 管理流程
创建账户 → 设积分 → 设计费规则 → 日常查记录 / 日志 / 统计。

---

## 四、本次存储变更说明（2026-07-26）

- **原来**：生图成功后后端下载图存火山 Supabase Storage，规避 5 天 URL 过期。
- **现在**：只存多米返回的图 URL，过期就过期（睿哥确认可接受，省存储成本）。
- **连带影响**：
  - 去掉 Storage 桶依赖，后端不再下载落盘，单次请求更轻、更不易超时。
  - 数据库仍用火山 Supabase **Postgres**（账户/积分/计费/记录/日志），不出境。
  - 前端"下载"改为**直链多米 URL**。
  - **图生图参考图（已定：Supabase 临时桶）**：多米只接受图片 URL（不支持 base64），用户上传参考图 → 前端传 Supabase Storage 临时桶 → 取公开 URL 传给多米 → 生成成功后删除（几 MB 几乎零成本）。

---

## 五、封板说明
  - 图生图参考图方案已定（Supabase 临时桶），其余功能与流程均已封板，无遗留。