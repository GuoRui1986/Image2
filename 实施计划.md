# 实施计划（编码会话开工依据）

> 本文档供**专门的编码会话**使用。当前讨论会话只做决策，不写应用代码。
> 本文与 `总体方案.md`（V5）、`两端功能与流程.md`、两个原型 HTML、`.env` 配套使用。
> 决策链以本计划 + V5 为准；V5 中"前端两个页面"等旧表述已被本计划"写在一起 SPA 单页"覆盖。

---

## 一、最新决策链（速查）

| # | 决策 | 结论 |
|---|------|------|
| 部署 | 火山引擎 **IGA Pages**（静态托管 + Node 边缘函数）+ **火山 Supabase**（Postgres，国内不出境） |
| 代码组织 | **单项目写在一起**（用户端 `/` 与管理端 `/admin` 同一 SPA） |
| 前端 | **原生 HTML/JS，SPA 单页 `index.html`**（视图切换，非多 HTML 文件） |
| 后端 | **Node.js + Express**（IGA Pages 仅支持 Node 运行时）；异步改**前端驱动轮询** |
| 登录 | 自建 `users` 表 + bcrypt + JWT，手机号+密码，无短信验证 |
| 管理员 | 单一，环境变量/种子脚本初始化 |
| 积分 | 内部虚拟额度，4 种生图方式各自设扣分 |
| 扣费 | **提交预扣、失败/部分失败返还**（行锁原子，防超额） |
| 生图方式 | **4 种**：`image2_t2i` / `image2_i2i` / `nano_t2i` / `nano_i2i` |
| 图存储 | **不落盘**，只存多米返回的 URL（约 5 天过期，接受） |
| 参考图 | 图生图参考图 → 前端传 **Supabase Storage 临时桶** → 取公开 URL 传给多米 → 生成成功后删除（几 MB 几乎零成本） |
| 数量 | 接口无 `n` 参数，前端**循环调用 `/api/generate` N 次**（每次只提交 1 个 task_id、独立轮询/计费/记录），避开边缘函数超时 |
| 节点 | gpt 暴露 `oversea`（默认海外）；nano `oversea` 待核实，不支持则固定国内 |

---

## 二、目录结构（单 IGA Pages 项目）

```
image2-platform/
├─ index.html              # SPA 单页（用户端 + 管理端视图切换）
├─ css/
│  └─ style.css
├─ js/
│  ├─ router.js          # hash 路由：#/login #/ #/admin #/me
│  ├─ api.js            # fetch 封装（自动带 JWT）
│  ├─ auth.js          # 登录态（localStorage token）、登出
│  └─ views/
│     ├─ user.js       # 用户端：工作台（文生图/图生图/批量/轮询/下载/个人中心）
│     └─ admin.js      # 管理端：7 菜单
├─ api/                 # IGA Pages Functions（Node.js Runtime）
│  ├─ [[default]].js   # 入口（IGA Pages 约定文件名：双括号=catch-all 路由），导出 Express 实例
│  ├─ db.js           # Supabase 客户端（service_role key）
│  ├─ auth.js         # 登录/me/中间件
│  ├─ admin.js        # 账户/积分/计费/记录/日志/统计
│  ├─ generate.js     # /api/generate 提交 + /api/status 轮询
│  ├─ domi.js        # 多米适配层（4 种方式，两套鉴权/查询）
│  └─ storage.js      # 参考图临时桶上传/删除
├─ sql/
│  └─ schema.sql      # 4 张表 + 种子（见第三节）
└─ .env                # 真实 Key，本地不提交（参见 .env.example）
```

> **部署形态已核实（IGA Pages / 火山 Supabase 官方文档，2026-07-26）**：
> - Functions 采用**文件系统路由**，`api/[[default]].js` 为 catch-all 入口，导出 Express 实例（`export default app`），平台接管监听。**后端用 Express 完全可行**，无需改架构。
> - 运行时选 **Node.js Runtime**（非 Edge）：bcrypt / @supabase/supabase-js 需 Node 环境；依赖用 `bcryptjs`（纯 JS，免原生编译）。
> - **火山引擎 Supabase 真实存在且 100% 兼容开源 Supabase** → 用 `@supabase/supabase-js` + service_role key；自带 Storage（参考图临时桶直接用）。
> - 部署后 `iga pages integration` 绑定 Supabase，**连接信息（URL/Key）自动注入为环境变量**；本地开发需在 `.env` 手动填 `SUPABASE_URL` / `SUPABASE_SERVICE_KEY`。
> - 火山 Supabase 当前为**公测**，需实名（企业/个人）账号才能开通。

---

## 三、Supabase 表结构（`sql/schema.sql`）

```sql
-- 1. 账户
create table users (
  id uuid primary key default gen_random_uuid(),
  phone text unique not null,
  password_hash text not null,
  balance integer not null default 0,
  role text not null default 'user',     -- admin | user
  status text not null default 'active',  -- active | frozen
  created_at timestamptz not null default now()
  deleted_at timestamptz,                -- 软删除：删除用户置时间戳，保留生图/操作记录外键
);

-- 2. 计费规则（4 种生图方式）
create table pricing_rules (
  id uuid primary key default gen_random_uuid(),
  type text unique not null,   -- image2_t2i|image2_i2i|nano_t2i|nano_i2i
  cost integer not null default 10
);
insert into pricing_rules (type, cost) values
  ('image2_t2i',10),('image2_i2i',10),('nano_t2i',10),('nano_i2i',10);

-- 3. 生图记录
create table generation_logs (
  id uuid primary key default gen_random_uuid(),
  task_id text unique not null,-- 多米 task_id（gpt 称 id），唯一约束防同一任务重复预扣/返还
  user_id uuid references users(id),
  method text not null,        -- image2_t2i|image2_i2i|nano_t2i|nano_i2i
  prompt text,
  ref_images jsonb,           -- URL 数组，图生图可选
  result_image text,           -- 多米返回 URL（5 天过期）
  cost integer not null default 0,
  status text not null,        -- success | fail
  oversea boolean default false,
  params jsonb,                -- 比例/尺寸/数量等留痕
  created_at timestamptz not null default now()
);

-- 4. 操作日志
create table operation_logs (
  id uuid primary key default gen_random_uuid(),
  admin_id uuid references users(id),
  action text not null,        -- create_user|freeze|unfreeze|reset_pwd|delete_user|adjust_credit|update_pricing
  target_type text,            -- user | pricing
  target_id text,
  detail text,
  created_at timestamptz not null default now()
);
```

**种子管理员**：用环境变量 `ADMIN_PHONE` / `ADMIN_PASSWORD`，服务首次启动（或一次性脚本）时若 `users` 无 admin 则插入。`password_hash` 用 bcrypt 生成。

---

## 四、环境变量（`.env`，已含 DOMI_KEY）

```
DOMI_KEY=            # 多米 API Key（已在 .env 落地）
SUPABASE_URL=        # 火山 Supabase 项目 URL
SUPABASE_SERVICE_KEY= # service_role key（后端用，绕过 RLS）
JWT_SECRET=          # 自建 JWT 签名密钥
ADMIN_PHONE=        # 种子管理员手机号
ADMIN_PASSWORD=      # 种子管理员密码
```

> 注：火山 Supabase 的开通入口/产品名实施时按控制台实际界面操作；RLS 建议关闭或用 service_role 直连（内部工具）。

---

## 五、后端边缘函数模块

### 5.1 路由清单
| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | `/api/auth/login` | 手机号+密码 → JWT | 公开 |
| GET | `/api/auth/me` | 返回当前用户(含余额/role) | 登录 |
| GET | `/api/admin/users` | 账户列表 | admin |
| POST | `/api/admin/users` | 创建账户(手机号+密码) | admin |
| POST | `/api/admin/users/:id/freeze` `/unfreeze` `/delete` `/reset-pwd` | 账户操作 | admin |
| POST | `/api/admin/credits` | 调整积分(delta+note)，写 operation_logs | admin |
| GET/PUT | `/api/admin/pricing` | 读/改 4 种计费 | admin |
| GET | `/api/admin/logs` | 操作日志 | admin |
| GET | `/api/admin/stats` | 使用统计聚合 | admin |
| GET | `/api/admin/records` | 生图记录(用户/方式/状态/时间筛选) | admin |
| POST | `/api/generate` | 提交多米，返回 task_id + method | 登录 |
| GET | `/api/status` | 轮询任务状态(按 method 调对应查询) | 登录 |
| POST | `/api/upload-ref` | 参考图传 Supabase 临时桶，返回 URL | 登录 |

### 5.2 扣费时机（关键）
- `/api/generate`：校验 JWT + 查 `cost` + 校验余额 ≥ `cost × 数量`，**原子预扣**（调用 RPC `deduct_points(user_id, cost)`，内部 `SELECT ... FOR UPDATE` 行锁 + 余额校验，余额不足抛异常）；提交多米拿 `task_id` 返回。
- `/api/status`：前端轮询触发。
  - `running` → 返回 `{status:'running'}`
  - `succeeded` → 凭 `task_id` 唯一约束保证仅处理一次：`INSERT` 成功记录（ON CONFLICT (task_id) DO NOTHING 直接返回，天然防重）；**预扣已扣，无需再扣**；返回 `{status:'success', url, cost}`
  - `failed` → 调用 RPC `refund_points(user_id, cost)` 返还积分；写 `generation_logs(fail)`；返回 `{status:'fail'}`
  - **数量 N（≤10）**：前端循环调用 `/api/generate` N 次，每次只提交 1 个 `task_id` 并各自轮询；**后端不批量同步提交**，避免边缘函数超时（与「前端驱动轮询」原则一致）。

### 5.3 多米适配层（`domi.js`，照 V5 §11 实抄）
抽象 `submit(method, payload)` / `query(method, taskId)` 按 method 分流：
- **nano 系列**（t2i / i2i）：
  - 提交 `POST https://duomiapi.com/api/gemini/nano-banana`（t2i）或 `/nano-banana-edit`（i2i）
  - Header `Authorization: <DOMI_KEY>`
  - 取 `data.task_id`
  - 查询 `GET https://duomiapi.com/api/gemini/nano-banana/result?id=<task_id>&key=<DOMI_KEY>`
  - 轮询至 `data.state=="succeeded"`，图取 `data.data.images[0].url`
- **gpt 系列**（t2i / i2i）：
  - 提交 `POST https://duomiapi.com/v1/images/generations?async=true`
  - Header `Authorization: <DOMI_KEY>`
  - 取顶层 `id`
  - 查询 `GET https://duomiapi.com/v1/tasks/<id>`（Header `Authorization`）
  - 轮询至 `state=="succeeded"`，图取 `data.images[0].url`
- **参数枚举**（详见 V5 §11）：
  - nano `model`：gemini-2.5-flash-image / gemini-3.1-flash-image-preview / gemini-3-pro-image-preview（或别名 nano-banana / nano-banana-2 / nano-banana-pro）
  - nano `image_size`：1K / 2K / 4K；`aspect_ratio`：auto/1:1/16:9/3:4...（默认 auto）
  - nano i2i `image_urls[]`：最多 10 张，仅 URL
  - gpt `size`：auto/1024x1024/1792x1024/16:9/自定义宽x高（宽高能被16整除，16–3840）；`quality`：low/medium/high；`oversea`：bool（默认 false=国内）；i2i 加 `image[]`
- **nano `oversea`**：文档未列，待核实；不支持则 nano 固定国内，节点切换仅对 gpt 生效。

### 5.4 参考图临时桶（`storage.js`）
- `POST /api/upload-ref`：接收前端上传文件 → 存 Supabase Storage 临时 bucket → 返回公开 URL。
- 生成成功后（`/api/status` succeeded 分支）：调 Supabase Storage 删除该 ref URL 对应文件（或定时清理）。

---

## 六、前端 SPA 结构

- `index.html`：单页，含登录层 + 应用容器；按 hash 路由切换视图。
- `js/router.js`：`#/login` → 登录；`#/` → 用户端工作台；`#/admin` → 管理端（需 admin role，否则跳登录）；`#/me` → 个人中心。
- `js/api.js`：fetch 封装，自动带 `Authorization: Bearer <JWT>`，统一错误处理。
- `js/auth.js`：登录态管理（localStorage 存 token + 用户信息），登出清 token。
- `js/views/user.js`：
  - 顶栏：手机号 + 剩余积分 + 登出
  - 引擎 Tab：image2 / nano
  - 模式：文生图 / 图生图（两引擎都支持）
  - 文生图表单：prompt(必填) · 模型(下拉) · 比例(下拉) · 尺寸(下拉) · 数量(1-10,前端循环调用 /api/generate) · 节点(国内/海外,默认海外) · 批量("加一条 prompt")
  - 图生图表单：多张参考图上传(Supabase 临时桶) · prompt · 模型 · 比例 · 尺寸 · 节点
  - 生成区：任务卡片列表（生成中/成功+下载/失败不扣/已过期），前端每 3–5 秒查 `/api/status`
  - 个人中心：剩余积分 + 我的历史（缩略图+prompt+方式+消耗+时间+下载）
  - 长 prompt：截断 2 行 + hover 完整 + 点击详情弹窗（参照原型）
- `js/views/admin.js`：7 菜单——仪表盘(趋势堆叠区分 image2/nano) / 账户管理 / 积分管理(列表+点调整弹窗) / 计费规则(4 输入) / 生图记录(筛选+表格) / 操作日志 / 使用统计。

> 视觉与交互细节以两个原型 HTML 为准（`原型-用户端.html` / `原型-管理端.html`）。

---

## 七、分步任务清单（可验收）

| Phase | 任务 | 验收 |
|--------|------|------|
| 0 | 建火山 Supabase 项目，跑 `schema.sql` 建 4 表+种子；建 IGA Pages 项目(git) | 表存在、admin 种子可登录 |
| 1 | 后端骨架：`index.js` + `db.js`（读 .env、Supabase 客户端） | 函数可启动、连库 |
| 2 | 鉴权：`/api/auth/login` + `/api/auth/me` + bcrypt + JWT + 种子 admin | 手机号密码登录拿 JWT，/me 返回信息 |
| 3 | 管理端 API：users CRUD/freeze/reset/delete、credits 调整+log、pricing、logs、stats、records | admin 端各接口可调用 |
| 4 | 多米适配层 `domi.js` + `/api/generate` + `/api/status`（含扣费时机、防重、写记录） | 单张生图全流程跑通 |
| 5 | 参考图临时桶 `/api/upload-ref` + 生成后删除 | 图生图多参考图可上传并出图 |
| 6 | 前端 SPA：登录 + 用户端工作台(参数/批量/轮询/下载/个人中心) + 管理端 7 页 | 浏览器走通两端 |
| 7 | 联调 + 真实 Key 跑通首图 | image2 文生图→nano 图生图(含多参考图)全链路验证 |

---

## 八、关键实现注意点（务必遵守）

1. **两套鉴权**：nano 提交用 `Authorization` 头、查询用 `?key=` 参数；gpt 全程 `Authorization` 头。
2. **两套查询接口**：nano `/result?id=&key=`；gpt `/v1/tasks/{id}`。返回结构不同（nano `data.data.images[]`、gpt `data.images[]`）。
3. **图 URL 5 天过期**：只存 URL，不落盘；前端直链下载。
4. **数量循环提交**：接口无 `n` 参数，前端循环调用 /api/generate N 次（N≤10），每次独立任务；后端不批量同步提交以免超时。
5. **提交预扣、失败返还**；用 `task_id` 唯一约束防重复预扣/返还，RPC 行锁防超额。
6. **前端驱动轮询**（边缘函数超时约束），每 3–5 秒查一次。
7. **bcrypt + JWT**；admin 路由校验 `role==admin`。
8. **参考图 Supabase 临时桶**，生成成功后删除。
9. **nano `oversea` 待核实**：不支持则 nano 固定国内节点。
10. 所有生图方式、参数名、返回字段**严格照 V5 §11**，不臆测。

---

## 九、验证方法（Phase 7）

用 `.env` 真实 `DOMI_KEY`：
1. 调 `/api/generate`（image2 文生图）→ 轮询 `/api/status` 至 success。
2. 确认 `generation_logs` 有 success 记录、余额扣减、前端可下载。
3. 走 nano 图生图（含多参考图上传到临时桶）→ 确认出图 + 临时图被删。
4. 走批量 + 数量=3，确认循环 3 次独立计费。
5. 失败场景确认返还积分。
6. 管理端：创建账户→设积分→改计费→查记录/日志/统计，全链路。

---

## 十、参考文件清单（新会话必读）

- `总体方案.md`（V5，接口契约 §11 必照抄）
- `两端功能与流程.md`
- `原型-用户端.html`（用户端视觉/交互基线）
- `原型-管理端.html`（管理端视觉/交互基线）
- `.env`（真实 Key，本地不提交）
- 本文件 `实施计划.md`
- 注：旧 Flask 草稿（app.py / static/index.html / requirements.txt / README.md）已删除，勿参考；按本计划 Node.js 方案实施。
