# 交接文档 —— 新会话开工必读

> 写于 2026-07-28。本文档是上一个规划/开发会话的完整交接，新会话读完本文件即可无缝续工。
> 阅读顺序：**本文件 → 实施计划.md → api/[[default]].js**（其余文件按需查阅）。

---

## 一、项目一句话

内部多人使用的 AI 生图平台：多米 API 中转调用 **gpt-image-2（image2）** 和 **nano banana**，含用户端（生图工作台）+ 管理端（账户/积分/计费/记录），部署在**火山引擎 IGA Pages（边缘函数 Node.js）+ 火山 Supabase（Postgres）**。

---

## 二、当前进度（关键！代码已写了大半）

### ✅ 已完成（Phase 0–7，已提交 git 并适配 IGA 部署）

| 模块 | 状态 | 说明 |
|---|---|---|
| 数据库 | ✅ | `sql/schema.sql` 已在火山 Supabase 执行 |
| 后端 | ✅ | `api/[[default]].js` 单入口，**Hono 框架**（Express 因 Edge Runtime 弃用），登录/生图提交/轮询/积分/管理接口全有 |
| 用户端 | ✅ | 已按设计图重构：卡片表单/方式卡片/toggle/滑块/结果卡片，图片下载走 fetch blob 绕跨域 |
| 管理端 | ✅ | 左侧导航 7 页（仪表盘/账户/积分/计费/记录/日志/统计）+ 统计接口，UI 已美化 |
| 4 种生图 | ✅ | image2 文生图/图生图（统一接口）、nano 文生图/图生图（旧专用接口）均跑通 |
| 图标 | ✅ | 全部 emoji 已换成内联 SVG 线性图标 |
| 部署 | ✅ | GitHub `GuoRui1986/Image2` → IGA Pages 自动构建；`@supabase/supabase-js` 锁 **2.43.0**（兼容 IGA Node 20/22） |

最后提交：`7fcfe95`（2026-07-27）。**未提交改动**：`实施计划.md`（新增策划器/hy3 章节）——交接时应已一并提交，开工前 `git status` 确认干净。

### ⏳ 待开发（新会话的活）

1. **Phase 8：电商详情图提示词策划器**（下一步主任务，实施计划第十一节有完整设计，接口已实测零悬念）
2. **Phase 8+ 生图能力增强清单**（实施计划第十二节）：批量 `n`、`output_format`、nano 迁统一接口+多档位、image2 多参考图 `images[]`、size 比例补全
3. PSD 导出：已调研，image2/多米均**不原生支持**，属远期后处理方案，暂不做

---

## 三、Phase 8 策划器核心决策（已封板，勿再讨论）

| 项 | 决策 |
|---|---|
| 功能定位 | 输出**提示词**而非直接出图（用户审完再去工作台生成，省真金白银的生图成本） |
| LLM | **腾讯云 TokenHub hy3**（`https://tokenhub.tencentmaas.com/v1/chat/completions`，Bearer `TOKENHUB_KEY`，OpenAI 兼容）。多米文本中转已否决 |
| 结构 | 固定 9 板块骨架（主图/场景/人群/细节/材质/尺寸/对比/资质/促销）+ LLM 填画面描述，`response_format: json_schema` 强约束 |
| 深度思考 | **不后端限制**，用户端「深度思考」toggle 自选（默认关）。开 → `thinking:{"type":"enabled"}` + `max_tokens ≥ 16000`；思考过程在 `reasoning_content`，前端折叠展示 |
| 衔接 | 板块卡「填入生图」按钮预填 prompt+size 到现有工作台，点生成才扣积分 |
| 计费 | `pricing_rules` 增 `detail_planner` 类型，单独定价（默认建议 5 分），成功才扣 |
| 新增文件 | 后端并入 `api/[[default]].js`（IGA 单入口，别新建 api 文件！）+ 前端策划视图 |

### ⚠️ 已实测踩掉的坑（编码必看）

- hy3 三项实测全通（普通调用/思考开关/json_schema），但 **json_schema 每个字段必须写 `description`**（如 `model: "取值只能是 image2 或 nano"`），否则 hy3 会把内容填串字段；system prompt 里再约束一遍枚举值。
- IGA Functions 只认 `api/[[default]].js` 单入口，业务代码全部内联，**不要拆多个 api 文件**（踩过坑，见 git log `02b75de`、`4600f5a`）。
- nano 走旧接口 `/api/gemini/nano-banana`，查询用 `?key=`；gpt 走 `/v1/tasks/{id}`，查询用 Authorization 头——两套不一样，别改混。

---

## 四、Key 与环境（都在本地 `.env`，已 gitignore）

| 变量 | 用途 |
|---|---|
| `DOMI_KEY` | 多米生图（jvYF…开头） |
| `TOKENHUB_KEY` | hy3 文本模型（sk-O2ax…开头，**已实测可用**） |
| `TOKENHUB_BASE_URL` | TokenHub 地址 |
| `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` | 火山 Supabase（IGA 部署时自动注入，本地开发手动填） |
| `JWT_SECRET` | 自建鉴权 |

线上环境变量在 IGA Pages 控制台配置；`TOKENHUB_KEY` 上线 Phase 8 时记得去 IGA 控制台补加。

---

## 五、文件地图

| 文件 | 作用 |
|---|---|
| `实施计划.md` | **主文档**：决策链、目录、SQL、路由、Phase 任务清单、第十一节策划器设计、第十二节增强清单 |
| `总体方案.md` / `两端功能与流程.md` | 早期定稿背景，一般不用再看 |
| `原型-用户端.html` / `原型-管理端.html` | 低保真原型（已按此实现，仅存档） |
| `api/[[default]].js` | 后端唯一入口（Hono） |
| `index.html` + `js/app.js` + `css/style.css` | 前端 SPA（用户端+管理端一体） |
| `sql/schema.sql` | 数据库结构（已执行；Phase 8 需补 `detail_planner` 计费种子） |
| `push.bat` | 一键 git push（推送即触发 IGA 自动部署） |

---

## 六、新会话开工指令（建议第一句话）

> 读 `交接文档.md` 和 `实施计划.md`，按 Phase 8 开发「电商详情图提示词策划器」：后端在 `api/[[default]].js` 加 `/api/planner` 路由（TokenHub hy3 + json_schema + thinking 透传 + detail_planner 计费），前端加策划视图（向导表单 + 9 板块卡片 + 深度思考 toggle + 一键填入生图），SQL 补计费种子。验收：填商品信息 → 出 9 板块提示词 → 填入生图跑通，思考开/关各测一次。

---

## 七、协作习惯（跟睿哥共事须知）

- 先对齐方向再动手，别自作主张扩需求；改 UI 前睿哥常给截图反馈，按截图改。
- 回复结构化、多用表格，话少事多。
- 对外操作（部署、push）可以直接干；大方向变更先问。
